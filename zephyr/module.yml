# Delete all unused properties.

# Give your module a name
name: hall-sensor-zmk-module
build:
  # Folder containing a "CMakeLists.txt" (. = root)
  cmake: .
  # Kconfig filepath
  kconfig: Kconfig
  settings:
    # Folder containing a "dts" folder (. = root)
    dts_root: .
    # Folder containing a "snippets" folder (. = root)
    snippet_root:  .
